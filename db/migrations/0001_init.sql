CREATE EXTENSION IF NOT EXISTS vector;
-- tables: users, devices, sources, snapshots, venues, event_candidates, events_canonical, event_instances, provenance,
-- user_preferences, feedback_events, follows, notification_log, embeddings(vector(1536))
